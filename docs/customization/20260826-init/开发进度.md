# Horizon 二开开发进度

> 追踪三个二开功能（自定义源 / 文章配图 / 文章上传）的实现进度。每完成一步即更新勾选。
> 完整方案见 [`实现细节.md`](实现细节.md)；开发纪律见根目录 [`CLAUDE.md`](../../../CLAUDE.md)。
> 分支：`feat/2026082622-init`

## 图例
- [ ] 未开始　🚧 进行中　[x] 已完成

---

## 阶段 0：脚手架 + 配置
- [x] `src/custom/__init__.py`
- [x] `src/custom/config.py`（pydantic 模型 + `load_custom_config`）
- [x] `tests/custom/`（目录 + `test_custom_config.py`，7 passed）
- [ ] `data/custom.json` 示例配置（阶段 3 收尾时补全全字段示例）

## 阶段 1：自定义源
- [x] `src/custom/scrapers/base.py`（CustomScraper 基类：RSS 伪装 + metadata 约定）
- [x] `src/custom/scrapers/example_source.py`（示例源 ExampleJSONScraper）
- [x] `src/custom/registry.py`（工厂表 + build_scraper）
- [x] `src/custom/hooks.py::append_custom_fetch_tasks`
- [x] Hook① 接线（`fetch_all_sources` + 顶部 import 标记）
- [x] `tests/custom/test_custom_scraper.py`（12 passed）

## 阶段 2：文章配图
- [x] `illustrator.py` anchor 计算 + 契约测试 `test_illustrator_anchor.py` ★（3 passed）
- [x] `hooks.py::insert_illustrations` + `test_insert_illustrations.py`（5 passed）
- [x] `illustrator.py` 生图/落盘/retention + `test_illustrator_flow.py`（4 passed）
- [x] `hooks.py::illustrate_items`
- [x] Hook②③ 接线（`run`：生图在 lang 循环前、插图在 summary 生成后）
- [x] **图像 provider 抽象层** `image_providers.py`（`image_provider` 字段切换）+ `test_image_providers.py`（6 passed）
  - `openai`：OpenAI 兼容 `/v1/images/generations`
  - `dashscope`：阿里云百炼文生图（异步提交 + 轮询，图片 URL 24h 过期即时落盘）

## 阶段 3：文章上传
- [x] `src/custom/publisher.py`（POST zh summary，日期 slug 幂等）
- [x] `hooks.py::publish_summary`
- [x] Hook④ 接线（`run`：webhook 之后）
- [x] `tests/custom/test_publisher.py`（5 passed）
- [x] `data/custom.json` 示例配置（全字段，默认全禁用）

## 阶段 4：收尾
- [x] `tests/custom/test_hooks_integration.py`（惰性缓存 + 降级，6 passed）
- [x] `grep -rn "HORIZON-CUSTOM" src/` 审计（import + 4 hook，共 5 处成对标记）
- [x] `uv run pytest` 回归通过（tests/custom 34 passed；上游测试无新增失败）
- [ ] 手动跑一次 `uv run horizon --hours 24` 验证端到端（需真实 API key，用户自测）

---

## 已知说明
- `tests/test_webhook.py`、`tests/test_extractors_trafilatura.py` 的失败是**沙箱网络环境**
  导致（URL 安全校验把测试地址解析为非公网 198.18.x.x），与二开无关；stash 二开改动后仍失败。
- 自定义 LLM Endpoint 为**纯配置**（上游 `AIConfig.base_url` 已支持），不在代码实现范围。
